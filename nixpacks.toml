# Nixpacks конфиг для Railway: явно требуем Node 22, чтобы tailwindcss/oxide и Next 16 завелись.
[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
